# تقرير تدقيق التكامل بين الفرنت والباك

تاريخ التدقيق: 12 سبتمبر 2026

## النطاق

- الفرنت: Astro 7 داخل `src/`.
- الباك: Go/Fiber داخل `back/`.
- تمت مراجعة الاتصال، مسارات API، شكل الاستجابات، الجلسات والكوكيز، الحماية بين الخادمين، CORS، CSRF، رفع الملفات، والمهل الزمنية.
- الكود الحالي هو المرجع الأساسي، وSwagger استُخدم كمرجع ثانوي فقط.

## الخلاصة

التكامل الأساسي صحيح من حيث البنية: الفرنت يعمل كطبقة BFF، ويضيف `X-Frontend-Key` و`X-Country-Id` من الخادم، والباك يعيد غلاف JSON متوافقًا عمومًا مع توقعات الفرنت. تسجيل الدخول، تحديث الجلسة، والرفع تمر عبر خادم Astro بدل كشف مفتاح الفرنت للمتصفح.

لا توجد مشكلة شاملة في عقود API تمنع التكامل. توجد مشكلة إعداد واحدة تمنع production build عند الاعتماد على `.env.example` كما كان، إضافة إلى عدة نقاط تقوية ومخاطر تشغيلية.

## النتائج حسب الأولوية

### 1. مانع إنتاج: `INTERNAL_API_URL` مطلوب لكنه كان غائبًا عن نموذج الإعداد

`src/lib/api.ts` يرفض production build عند غياب `INTERNAL_API_URL`، بينما `.env.example` لم يكن يعرّف هذا المتغير. كما أن الطلبات الخلفية تُبنى من هذه القيمة، ولذلك يجب أن تتضمن `/api`.

**الأثر:** أي نشر يعتمد على نموذج الإعداد كما كان سيتوقف أثناء البناء أو بدء التشغيل.

**الإجراء المنفذ:** أضيف `INTERNAL_API_URL=https://api.imanjo.com/api` إلى `.env.example` مع توضيح تفضيل عنوان داخلي خاص في الإنتاج.

### 2. متوسط مشروط: مفتاح الفرنت صلاحية عبور واسعة

الفرنت يضيف `X-Frontend-Key` إلى كل طلب خلفي في `src/lib/api.ts`. الباك يقبل المفتاح كأعلى مسار ثقة في `back/internal/middleware/frontend_guard.go`، كما يُستخدم لحماية نقاط تشغيل داخلية.

**الأثر المشروط:** إذا تسرّب المفتاح من bundle للمتصفح أو السجلات أو صفحة خطأ، فإنه يسمح بتجاوز بوابة Origin/Referer فقط؛ تبقى مصادقة المستخدم وصلاحياته مطبقة على المسارات المحمية. لم يظهر في التدقيق دليل على تسريب حالي.

**المطلوب:**

1. إبقاء `FRONTEND_API_KEY` متغيرًا سريًا غير مسبوق بـ `PUBLIC_`.
2. إضافة اختبار build يؤكد أن قيمة المفتاح لا تظهر في ملفات المتصفح.
3. فصل مفتاح نقاط التشغيل الداخلية مثل health/metrics عن مفتاح BFF.
4. تدوير المفتاح فور الاشتباه بتسريبه.

### 3. متوسط مشروط: حماية CSRF موجودة في Astro وليست دفاعًا مستقلاً في الباك

`src/middleware.ts` يرفض عمليات التعديل إذا لم يطابق `Origin` أو `Referer` الموقع المسموح. في المقابل، `FrontendGuard` في الباك يسمح للطلب الحامل لكوكي أو token بالعبور دون فحص CSRF مستقل.

**التقييم:** لا تمثل هذه ثغرة حالية مثبتة؛ `FrontendGuard` يحجب المضيف العام في الإنتاج قبل مسار الكوكي، والكوكيز `SameSite=Lax`، وAstro يطبق فحصًا مستقلًا. إضافة CSRF في الباك دفاع إضافي مهم عند وجود مضيف داخلي بديل أو تهديدات same-site أو خطأ في إعداد الشبكة.

**المطلوب:**

1. إبقاء API خلف BFF أو تقييده شبكيًا.
2. إضافة فحص Origin/Fetch-Metadata أو CSRF token في الباك لعمليات POST/PUT/PATCH/DELETE المعتمدة على الكوكيز.
3. عدم تخفيف فحص Astro الحالي.

### 4. منخفض — تقوية: CORS يدرج اسم هيدر المفتاح السري

`back/internal/middleware/cors.go` يسمح باسم `X-Frontend-Key` مع `AllowCredentials: true`. هذا لا يكشف قيمة المفتاح ولا يمنحها للمتصفح، كما أن origins غير المسموحة تُرفض. مع ذلك، السماح بالهيدر غير مطلوب في تصميم BFF.

**المطلوب:** إزالة `X-Frontend-Key` من `AllowHeaders` الخاصة بالمتصفح، ورفض wildcard، والتحقق عند بدء الباك من أن origins هي عناوين HTTPS كاملة ومحددة.

### 5. متوسط: OAuth المباشر لا يتوافق مع Preview بالإعدادات الحالية

مثال إعداد الباك يسمح فقط بـ `imanjo.com` و`www.imanjo.com`. تسجيل الدخول العادي وsocial token login يمكن أن يعملا لأن Astro يستخرج tokens ويعيد إصدار كوكيز host-local. عدم التوافق يخص تدفق OAuth المباشر: callback في الباك يعيد التوجيه إلى `FRONTEND_URL` ويصدر كوكيز للنطاق المضبوط.

**الأثر:** OAuth redirect/callback لن يعود إلى Preview أو ينشئ جلسة Preview ما لم توجد إعدادات تطوير منفصلة.

**المطلوب:** في التطوير، تشغيل الباك والفرنت عبر أصل واحد أو استخدام كوكيز host-only محلية، وعدم إضافة نطاقات Preview إلى سياسة الإنتاج الدائمة.

### 6. متوسط: الثقة في عناوين IP المحوّلة تحتاج ضبط reverse proxy

الفرنت يمرر `X-Forwarded-For` وUser-Agent وReferer. الباك يستخدم عنوان العميل ضمن اكتشاف SSR والحد من الطلبات.

**المطلوب:** يجب على nginx استبدال هيدرات forwarding الواردة من الإنترنت، وقبولها فقط من proxies موثوقة. لا ينبغي اعتبار User-Agent وحده دليل SSR.

### 7. متوسط: تسجيل الخروج محلي حتى عند فشل الإلغاء في الباك

Astro يمسح الكوكيز محليًا حتى إذا فشل طلب logout في الباك.

**الأثر:** قد يبقى token صالحًا في مكان آخر إلى أن تنتهي مدته.

**المطلوب:** تسجيل ومراقبة فشل إلغاء الجلسة في الباك، وإعادة المحاولة أو إظهار نتيجة مناسبة بدل اعتباره نجاحًا كاملاً بصمت.

### 8. متوسط: المهل الزمنية غير مناسبة لبعض عمليات AI والتدقيق

المهلة الافتراضية في `src/lib/api.ts` هي 10 ثوانٍ، والرفع 30 ثانية فقط عند اختيار الثابت صراحة. بعض مسارات AI وcontent audit قد تتجاوز ذلك.

**المطلوب:** تحديد مهلة واضحة لكل عملية طويلة، ويفضل تحويل الأعمال الطويلة إلى job يعيد معرفًا ثم polling للحالة.

### 9. منخفض/متوسط: رسائل الباك تُمرر أحيانًا إلى URL

بعض BFF routes تضع `json.message` في query string بعد redirect.

**الأثر:** قد تظهر تفاصيل غير متوقعة في history وReferer والسجلات.

**المطلوب:** تحويل أخطاء الباك إلى رموز ورسائل واجهة ثابتة، وإضافة `Cache-Control: no-store` لاستجابات المصادقة والأخطاء الحساسة.

### 10. منخفض: القوائم قد تخفي فشل API كقائمة فارغة

`apiFetchList` يعيد `items: []` عند الفشل مع الاحتفاظ بـ `ok: false`. أي مستهلك لا يفحص `ok` قد يعرض "لا توجد بيانات" بدل خطأ اتصال.

**المطلوب:** التأكد أن كل صفحات القوائم تفحص `ok`، وإضافة اختبار يمنع عرض الحالة الفارغة عند 5xx أو timeout.

## نقاط التوافق المؤكدة

- مسارات الفرنت تضيف `/auth/*` و`/dashboard/*` إلى base المنتهي بـ `/api`، وهو متوافق مع مجموعات routes في Go.
- غلاف الاستجابة المتوقع `{ success, data, message, meta|pagination }` متوافق مع `back/internal/utils/response.go`.
- تحديث الجلسة متوافق: الباك يقبل refresh token من JSON أو كوكي، والفرنت يمرر كوكي `refresh_token`.
- كوكيز الطرفين `HttpOnly` و`SameSite=Lax` و`Secure` في الإنتاج.
- الفرنت يرسل `X-Country-Id`، والباك يستخدمه لاختيار قاعدة بيانات الدولة.
- رفع الملفات يمر عبر BFF، والفرنت لا يضبط `Content-Type` يدويًا للـ multipart.
- الباك يدعم POST في تعديلات multipart التي لا يتعامل معها Fiber جيدًا عند PUT.
- الباك يتحقق من blacklist و`auth_version` وحالة المستخدم عند المصادقة.

## متطلبات الإعداد المشتركة

يجب أن تتطابق القيم التالية بين الطرفين:

| الفرنت | الباك | الملاحظة |
|---|---|---|
| `FRONTEND_API_KEY` | مفتاح Frontend API | تطابق حرفي، وسري في الطرفين |
| `PUBLIC_API_URL` | عنوان API العام | يجب أن ينتهي الفرنت بـ `/api` |
| `INTERNAL_API_URL` | عنوان وصول داخلي للباك | مطلوب في الإنتاج، ويجب أن يتضمن `/api` |
| `PUBLIC_SITE_URL` | `FRONTEND_URL` | نفس الأصل الفعلي للموقع |
| أصل الموقع | `CORS_ALLOWED_ORIGINS` وFrontend origins | قائمة محددة دون wildcard |

## فجوات الاختبار المقترحة

1. اختبار contract آلي يجمع routes من Go ويقارنها بالمسارات النصية المستخدمة في Astro.
2. اختبار login/refresh/logout حقيقي بين الخدمتين مع الكوكيز.
3. اختبار multipart لكل رفع وتعديل يحتوي ملفات.
4. اختبار 401 و403 و404 و422 و429 و5xx وtimeout وinvalid JSON.
5. اختبار يمنع وجود `FRONTEND_API_KEY` في browser assets.
6. اختبار CSRF مباشر على الباك، وليس على Astro فقط.

## نتيجة التحقق الحالية

- تم تشغيل اختبارات الفرنت على Node 22: نجحت 19 من 19.
- تم تشغيل `astro check`: صفر أخطاء، صفر تحذيرات، وصفر ملاحظات.
- وصل production build إلى prerender ثم توقف بسبب غياب `PUBLIC_API_URL` و`INTERNAL_API_URL` و`FRONTEND_API_KEY` و`PUBLIC_SITE_URL`. هذا سلوك fail-fast مقصود، ولم تُستخدم قيم أو أسرار وهمية لتجاوزه.
- تعذر إكمال compile/tests للباك داخل بيئة Replit لأن سياسة الحزم حجبت تنزيل `golang.org/x/crypto@v0.50.0` بسبب تصنيف ثغرة حرجة. هذا عائق تحقق واعتماد يجب معالجته بترقية dependency إلى إصدار مسموح وآمن، وليس بتجاوز سياسة الحماية.